from setuptools import setup, find_packages

setup(
    name="agent_v0",
    version="0.1.0",
    description="Self-contained modular agent system",
    author="davidebaldelli",
    packages=find_packages(),
    include_package_data=True,
    install_requires=[],
)